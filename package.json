{
	"name": "@alexsasharegan/middleware",
	"version": "1.0.0",
	"description": "A set of custom middleware.",
	"main": "lib/index.js",
	"scripts": {
		"build": "rm -Rf lib; tsc",
		"prepublishOnly": "npm run build",
		"release": "standard-version",
		"postrelease": "git push --follow-tags origin master && npm publish",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "Alex Regan <npm@33mail.alexregan.com>",
	"license": "MIT",
	"peerDependencies": {
		"express": "latest"
	},
	"devDependencies": {
		"@types/express": "^4.17.1",
		"@types/node": "^11.13.20",
		"express": "^4.17.1",
		"prettier": "^1.18.2",
		"standard-version": "^5.0.2",
		"typescript": "^3.6.3"
	},
	"dependencies": {
		"chalk": "^2.4.2",
		"fmt-bytes": "^1.0.2"
	}
}
